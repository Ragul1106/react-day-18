const AxiosIntro = () => (
  <div>
    <h2>Axios Introduction</h2>
    <p>
      Axios is a promise-based HTTP client for the browser and Node.js. It provides a cleaner API, interceptors, automatic JSON transformation, and better error handling compared to `fetch()`.
    </p>
  </div>
);

export default AxiosIntro;
