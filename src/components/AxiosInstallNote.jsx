const AxiosInstallNote = () => (
  <div>
    <h2>Install Axios</h2>
    <p>Run <code>npm install axios</code> in your React app to start using Axios.</p>
  </div>
);

export default AxiosInstallNote;
